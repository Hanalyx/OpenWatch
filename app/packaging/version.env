# OpenWatch Go rebuild — single source of truth for version metadata.
#
# Distinct from the legacy Python project's ../packaging/version.env.
# The Go binary's ldflags read this file via the app/Makefile; build
# scripts in app/packaging/{rpm,deb}/ source it for spec macros.
VERSION="0.2.0-rc.1"
CODENAME="Eyrie"
