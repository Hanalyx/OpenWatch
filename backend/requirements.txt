# OpenWatch Backend Dependencies
# Python Version Requirement: 3.12+ (targeting FreeBSD 15.0)
# Security Compliance: NIST SP 800-53, FedRAMP, CMMC, ISO 27001, PCI DSS
# FIPS 140-2: OpenSSL 3.x FIPS provider (portable, not tied to Red Hat)

# Core web framework
fastapi==0.129.0
uvicorn[standard]==0.40.0
starlette==0.52.1  # Security: CVE-2025-62727 fixed
python-multipart==0.0.22

# Database (PostgreSQL only)
SQLAlchemy==2.0.46
alembic==1.18.4
psycopg2-binary==2.9.11
asyncpg==0.31.0


# Authentication & Security
PyJWT==2.11.0
passlib==1.7.4
bcrypt==5.0.0
argon2-cffi==25.1.0
pyotp==2.9.0
qrcode==7.4.2
cryptography==46.0.5

# SSH
paramiko==3.5.0

# HTTP Client (consolidated — requests and aiohttp removed)
httpx==0.28.1
aiohttp==3.13.3  # Keep: Kensa updater plugin depends on it

# Data Validation
pydantic==2.12.5
pydantic-settings==2.13.0
email-validator==2.3.0

# Configuration
python-dotenv==1.2.1
PyYAML==6.0.3
Jinja2==3.1.6  # Report HTML templating

# Observability
opentelemetry-api==1.39.1
opentelemetry-sdk==1.39.1
opentelemetry-instrumentation-fastapi==0.60b1
opentelemetry-instrumentation-sqlalchemy==0.60b1
opentelemetry-exporter-otlp==1.39.1
prometheus-client==0.24.1
psutil==7.2.2

# Email
aiosmtplib==5.1.0

# Caching (replaces Redis rule cache)
cachetools>=5.5.0

# Utilities
python-dateutil==2.9.0.post0
pytz==2025.2
semver==3.0.4
slack-sdk>=3.27.0

# SSO Federation
authlib>=1.3.0
pysaml2>=7.5.0

# Kensa compliance engine
kensa @ git+https://github.com/Hanalyx/kensa.git@v1.2.5
