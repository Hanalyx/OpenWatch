import React from 'react';
import { Container, Typography } from '@mui/material';

const Content: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">SCAP Content Library</Typography>
    </Container>
  );
};

export default Content;