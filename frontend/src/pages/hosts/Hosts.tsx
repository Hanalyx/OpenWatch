import React from 'react';
import { Container, Typography } from '@mui/material';

const Hosts: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">Hosts Management</Typography>
    </Container>
  );
};

export default Hosts;
