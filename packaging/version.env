# OpenWatch — single source of truth for version metadata.
#
# The Go binary's ldflags read this file via the Makefile; build scripts
# in packaging/{rpm,deb}/ source it for spec macros.
VERSION="0.3.0"
CODENAME="Eyrie"
